{
  "name": "cf-starter",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev ./frontend/dist --compatibility-date 2024-01-01 --d1 DB --kv KV --r2 BUCKET -- npm run dev:frontend",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "deploy": "npm run build && wrangler pages deploy ./frontend/dist",
    "db:migrate": "wrangler d1 execute DB --local --file=./migrations/0001_init.sql",
    "db:migrate:remote": "wrangler d1 execute DB --remote --file=./migrations/0001_init.sql"
  },
  "devDependencies": {
    "wrangler": "^4.71.0"
  }
}
